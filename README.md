# Assignment 8 — Django  + MongoDB

## Overview

This Django web application allows users to input the MAC Address and DHCP VERSION, to simulate DHCPv4 or DHCPv6, generate/validate IPv4 or IPv6 addresses, saving all input + assigned IPs to a MongoDB database hosted on a separate EC2 instance running Ubuntu

---

## Technologies Used

- Django
- Djongo (MongoDB backend for Django ORM)
- HTML Template

